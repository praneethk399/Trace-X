# TraceX — Backend environment
# Copy to .env and fill in. Never commit the real .env.

# Port for the Express API server (frontend proxies /api here)
PORT=8787

# Supabase project (already wired in code as defaults; override here if changed)
SUPABASE_URL=https://dtrgsngsgklzqibchqpn.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_NpwSdNWaPzedmNhzoe2pdQ_Qo6uRHuS

# Gemini AI (optional — risk explanations fall back to the deterministic generator)
GEMINI_API_KEY=

# Vite: where the dev server proxies /api
API_PROXY_TARGET=http://localhost:8787
